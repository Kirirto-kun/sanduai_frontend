# Local development. In production this value is a Docker build argument baked
# into the browser bundle; runtime environment changes cannot replace it.
# localhost, loopback addresses, and HTTP are rejected by a production build.
NEXT_PUBLIC_API_BASE=http://127.0.0.1:8000
