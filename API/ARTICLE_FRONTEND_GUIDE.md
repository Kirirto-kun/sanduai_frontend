# 📰 Статьи (Мақала) - Инструкция для Frontend

## Три простых шага

### 1️⃣ Создать статью → 2️⃣ Редактировать → 3️⃣ Скачать DOCX

---

## 1. Генерация статьи

**POST** `/api/article/generate`

### Отправляешь:
```json
{
  "topic": "Цифровизация образования в РК",
  "language": "rus",                    // "kaz" или "rus"
  "author_name": "Айгүл Омарова",
  "author_role": "Учитель информатики",
  "genre": "scientific",                // "scientific", "publicistic", "custom"
  "custom_genre_description": null,     // Если genre="custom", здесь описание
  "additional_context": "Упомяни платформу Bilimland"  // Опционально
}
```

### Жанры:

| genre | Что получишь |
|-------|--------------|
| `scientific` | Научная статья: аннотация + ключевые слова + список литературы |
| `publicistic` | Публицистика: живой язык, без литературы |
| `custom` | Свой вариант: "Пост для Инстаграм", "Доклад завучу" и т.д. |

### Получаешь:
```json
{
  "meta": {
    "title": "Цифровизация образования: опыт внедрения платформы Bilimland",
    "author_block": "Айгүл Омарова, учитель информатики",
    "abstract": "В статье рассматривается опыт внедрения...",
    "keywords": ["цифровизация", "Bilimland", "образование", "РК"]
  },
  "sections": [
    {
      "heading": "Введение",
      "content": "Цифровая трансформация образования является..."
    },
    {
      "heading": "Методология",
      "content": "Исследование проводилось в течение..."
    },
    {
      "heading": "Результаты",
      "content": "Анализ показал, что использование Bilimland..."
    }
  ],
  "conclusion": "Таким образом, внедрение платформы Bilimland...",
  "references": [
    "Министерство просвещения РК. Программа Біртұтас тәрбие, 2021",
    "Иванов И.И. Цифровые технологии в образовании, 2022"
  ]
}
```

⏱️ **15-40 секунд**

**Поле `additional_context` - ВАЖНО!**  
Если написать "Упомяни мой 7 'А' класс", статья будет про этот класс, а не общая!

---

## 2. Редактура статьи

**POST** `/api/article/revise`

### Два способа править:

#### A) Точечные правки (несколько сразу)

```json
{
  "current_meta": {...текущие метаданные...},
  "current_sections": [...текущие разделы...],
  "current_conclusion": "текущее заключение",
  "current_references": [...],
  "inline_comments": [
    {
      "target_text": "платформа Билимленд",
      "instruction": "замени на 'образовательная платформа Bilimland'"
    },
    {
      "target_text": "показали",
      "instruction": "замени на 'продемонстрировали'"
    }
  ]
}
```

#### B) Общая правка

```json
{
  "current_meta": {...},
  "current_sections": [...],
  "current_conclusion": "...",
  "general_instruction": "Сделай стиль более научным, добавь ссылки на стандарты РК"
}
```

### Получаешь:
```json
{
  "meta": {...обновленные метаданные...},
  "sections": [...обновленные разделы...],
  "conclusion": "...",
  "references": [...]
}
```

**ВАЖНО:** AI меняет ТОЛЬКО то, что ты попросил!

⏱️ **15-40 секунд**

---

## 3. Скачать DOCX

**POST** `/api/article/export/docx`

### Отправляешь:
```json
{
  "meta": {
    "title": "Заголовок",
    "author_block": "ФИО, должность",
    "abstract": "Аннотация...",
    "keywords": ["ключ1", "ключ2"]
  },
  "sections": [
    {"heading": "Введение", "content": "..."},
    {"heading": "Основная часть", "content": "..."}
  ],
  "conclusion": "Заключение...",
  "references": ["1. Источник", "2. Источник"]
}
```

### Получаешь:
Файл `article.docx` с форматированием:
- ✅ Times New Roman
- ✅ Заголовок: жирный, центр, ПРОПИСНЫЕ, 16pt
- ✅ Автор: курсив, справа, 14pt
- ✅ Аннотация: 12pt
- ✅ Ключевые слова: курсив, 12pt
- ✅ Список литературы: нумерованный, 12pt

⏱️ **1-3 секунды**

---

## 💻 Код для копирования

```javascript
const API_URL = 'http://localhost:8000';
const token = localStorage.getItem('token');

// 1. Генерация
async function generateArticle(topic, genre, authorName, authorRole, language) {
  const res = await fetch(`${API_URL}/api/article/generate`, {
    method: 'POST',
    headers: {
      'Authorization': `Bearer ${token}`,
      'Content-Type': 'application/json'
    },
    body: JSON.stringify({
      topic,
      language,
      author_name: authorName,
      author_role: authorRole,
      genre,
      custom_genre_description: genre === 'custom' ? 'Пост для Инстаграм' : null,
      additional_context: null  // Или текст от пользователя
    })
  });
  return await res.json();
}

// 2. Редактура
async function reviseArticle(meta, sections, conclusion, refs, comments) {
  const res = await fetch(`${API_URL}/api/article/revise`, {
    method: 'POST',
    headers: {
      'Authorization': `Bearer ${token}`,
      'Content-Type': 'application/json'
    },
    body: JSON.stringify({
      current_meta: meta,
      current_sections: sections,
      current_conclusion: conclusion,
      current_references: refs,
      inline_comments: comments
    })
  });
  return await res.json();
}

// 3. Скачать
async function downloadArticleDocx(meta, sections, conclusion, refs) {
  const res = await fetch(`${API_URL}/api/article/export/docx`, {
    method: 'POST',
    headers: {
      'Authorization': `Bearer ${token}`,
      'Content-Type': 'application/json'
    },
    body: JSON.stringify({
      meta,
      sections,
      conclusion,
      references: refs
    })
  });
  
  const blob = await res.blob();
  const url = URL.createObjectURL(blob);
  const a = document.createElement('a');
  a.href = url;
  a.download = 'article.docx';
  a.click();
  URL.revokeObjectURL(url);
}
```

---

## 🎨 UI подсказки

### Выбор жанра:

```
┌─────────────────────────────────────┐
│ Выберите тип статьи:                │
│                                     │
│ ○ Научная статья                    │
│   (аннотация + литература)          │
│                                     │
│ ○ Публицистическая статья           │
│   (живой язык, без литературы)      │
│                                     │
│ ○ Свой вариант                      │
│   [Пост для Инстаграм    ]          │
│                                     │
│ Дополнительная информация:          │
│ [Упомяни платформу Bilimland ]      │
│ [и опыт работы с 7 'А' классом]     │
└─────────────────────────────────────┘
```

### После генерации:

```
┌──────────────────────────────────────┐
│ ЦИФРОВИЗАЦИЯ ОБРАЗОВАНИЯ             │
│ (жирный, центр, прописные)           │
│                                      │
│           Айгүл Омарова,             │
│      учитель информатики             │
│         (курсив, справа)             │
│                                      │
│ Аннотация: В статье рассматрив...   │
│ Ключевые слова: цифровизация, ...   │
│                                      │
│ ────────────────────────────────     │
│                                      │
│ ВВЕДЕНИЕ                             │
│ [редактируемое поле]                 │
│                                      │
│ МЕТОДОЛОГИЯ                          │
│ [редактируемое поле]                 │
│                                      │
│ ЗАКЛЮЧЕНИЕ                           │
│ [редактируемое поле]                 │
│                                      │
│ СПИСОК ЛИТЕРАТУРЫ                    │
│ 1. [источник]                        │
│ 2. [источник]                        │
│                                      │
│ [🔄 Применить правки]  [💾 DOCX]     │
└──────────────────────────────────────┘
```

---

## 📊 Что выбрать?

| Задача | Жанр | Что будет |
|--------|------|-----------|
| Диплом, курсовая | `scientific` | Аннотация, ключевые слова, список литературы |
| Статья в газету | `publicistic` | Живой язык, примеры, без литературы |
| Пост для соцсетей | `custom: "Пост для Инстаграм"` | Эмодзи, короткие абзацы |
| Доклад на педсовет | `custom: "Доклад"` | Структурировано, без аннотации |

---

## ⚠️ Ошибки

| Статус | Что случилось | Что делать |
|--------|---------------|------------|
| 401 | Токен невалиден | Перелогиниться |
| 502 | AI не ответил | Попробовать снова |
| 400 | Неправильные данные | Проверить поля |

---

## ✅ Чеклист для фронтенда

- [ ] Форма: тема, язык, автор, жанр
- [ ] Показать поле custom_genre_description если выбран "Свой вариант"
- [ ] Поле "Дополнительная информация" (опционально)
- [ ] Loading 15-40 сек при генерации
- [ ] Показать: метаданные + разделы + заключение + литература
- [ ] Возможность редактировать вручную
- [ ] Точечные правки (выделение → popup)
- [ ] Кнопка "Скачать DOCX"

---

**Готово!** Всё что нужно для интеграции ✅

